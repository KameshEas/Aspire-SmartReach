import LoginForm from "@/components/sections/LoginForm";

export const metadata = {
  title: "Sign In — Aspire SmartReach",
  description: "Sign in to Aspire SmartReach to manage your automations.",
};

export default function Page() {
  return <LoginForm />;
}
